项目目录
my-nest/
├── src/
│ ├── main.ts # 应用入口
│ ├── app.module.ts # 根模块 应用的入口，负责组织模块、控制器和提供者（services）
│ ├── app.controller.ts # 根控制器 处理 HTTP 请求、返回响应（把请求转给服务或直接返回）对应express中的路由层
│ └── app.service.ts # 根服务 ：放业务逻辑（数据处理、计算、调用外部资源），供控制器或其他提供者调用。对应express业务逻辑层
├── test/ # 测试文件
├── nest-cli.json # Nest 配置
├── package.json
└── tsconfig.json

运行流程
1.Nest 启动时加载 AppModule，在内部创建依赖注入容器并注册 AppController 与 AppService。2.当有 HTTP 请求（例如 GET /）到来时，Nest 找到 AppController 上对应的处理方法 getHello() 并执行。
3.AppController.getHello() 调用通过构造函数注入进来的 AppService.getHello()。
4.AppService.getHello() 执行业务逻辑并返回结果（这里返回字符串）。5.控制器把结果作为 HTTP 响应返回给客户端。

分层的好处
可维护性：控制器只负责“接收并响应请求”，服务负责“做事情/业务逻辑”，职责清晰。
可测试性：服务可以单独测试（不依赖 HTTP），控制器也可以只 mock 服务来测试路由逻辑。
可重用与解耦：服务可以被多个控制器或其他服务复用。

常用装饰器速查表

<!-- 装饰器                  说明                       示例
@Controller('prefix') 定义控制器和路由前缀     @Controller('users')
@Get('path')          GET 请求                @Get(':id')
@Post()               POST 请求               @Post()
@Put()                PUT 请求                @Put(':id')
@Delete()             DELETE 请求             @Delete(':id')
@Param('key')         获取路由参数             @Param('id') id
@Query('key')         获取查询参数             @Query('page') page
@Body()               获取请求体               @Body() data
@Req()                获取完整 Request 对象    @Req() req
@Res()                获取完整 Response 对象   @Res() res -->

Injectable 是用来标记一个类可以被 Nest 的依赖注入系统管理的装饰器。它告诉 Nest 这个类是个提供者，可以在其他地方被注入和使用。通过使用 @Injectable()，你可以将这个类注册为一个服务，并在需要的地方通过构造函数注入来使用它。这是 Nest.js 中实现依赖注入的一种方式，使得代码更加模块化和可测试。

Controller 是 Nest.js 中的一个装饰器，用于定义一个控制器类。控制器负责处理传入的 HTTP 请求，并返回响应。通过使用 @Controller() 装饰器，你可以指定这个控制器处理的路由路径。例如，@Controller('users') 表示这个控制器将处理以 /users 开头的请求。

Get 装饰器用于处理 HTTP GET 请求，Post 装饰器用于处理 HTTP POST 请求，Delete 装饰器用于处理 HTTP DELETE 请求，Put 装饰器用于处理 HTTP PUT 请求。Param 装饰器用于获取 URL 中的参数，Query 装饰器用于获取查询参数，Body 装饰器用于获取请求体中的数据。

@Module 是 Nest.js 中的一个装饰器，用于定义一个模块类。模块是 Nest.js 中的一个重要概念，它用于组织代码和提供依赖注入的上下文。通过使用 @Module() 装饰器，你可以指定这个模块包含哪些控制器、提供哪些服务，以及导入哪些其他模块。在这个例子中，AppModule 包含了 AppController 和 AppService，并且没有导入其他模块。

@Module 中的 imports 数组用于导入其他模块，controllers 数组用于注册控制器，providers 数组用于注册提供者（服务）。在这个例子中，AppModule 没有导入其他模块，但注册了 AppController 和 AppService。这样，AppController 就可以通过依赖注入使用 AppService 中定义的方法。

nest g module users # 生成 users 模块
nest g controller users --no-spec # 生成控制器（不生成测试文件）
nest g service users --no-spec # 生成服务

DTO
dot 是 Data Transfer Object 的缩写，表示数据传输对象。它是一种设计模式，用于在不同层之间传递数据。在 NestJS 中，DTO 通常用于定义请求体的数据结构，以确保数据的有效性和类型安全。

在 NestJS 中，DTO 通常是一个简单的类，包含属性和类型定义。它可以使用装饰器来验证数据，例如使用 class-validator 库中的装饰器来验证请求体中的数据是否符合预期的格式和要求。

DTO（Data Transfer Object）是一个概念/模式：用类或接口来描述“数据的形状”（比如请求体应该包含哪些字段、字段类型是什么）。它是静态的“契约”，在代码中通常表现为一个 TypeScript 类或接口。

class-validator 是一个运行时验证库：通过在 DTO 类的属性上加装饰器（如 @IsString、@IsNotEmpty、@IsEmail 等），在运行时检查实际传入的数据是否满足 DTO 所声明的规则。

class-transformer
作用：把 plain object（如 JSON / 请求体）和 class 实例互相转换；做序列化/反序列化、类型转换、字段暴露/隐藏（@Expose / @Exclude）、自定义转换（@Transform）等。
运行时行为：读取类的元数据（例如 @Type）并生成新的实例或 plain object。
常用 API：plainToInstance（以前叫 plainToClass）、instanceToPlain、classToPlain 等。

class-validator
作用：基于类及其属性上的验证装饰器（@IsString、@IsInt、@ValidateNested 等）在运行时校验对象是否满足规则，返回验证错误。
运行时行为：读取由装饰器产生的元数据，进行断言/校验（validate / validateSync，或由 Nest 的 ValidationPipe 自动调用）。
常用 API：validate、validateSync，以及大量装饰器（@IsNotEmpty、@IsEmail、@Min、@Max…）。

ValidationPipe 是 Nest.js 提供的一个内置管道，用于验证和转换请求数据。它可以帮助你确保传入的数据符合预期的格式和类型，并且可以自动将请求体转换为 DTO（数据传输对象）类实例。通过使用 ValidationPipe，你可以轻松地实现数据验证和转换，提升应用的健壮性和安全性。
在 main.ts 中使用 ValidationPipe，可以全局应用数据验证和转换逻辑，确保所有请求的数据都经过验证和转换处理。这有助于防止无效数据进入应用，并且可以简化控制器中的代码，使其更专注于业务逻辑。
参数
new ValidationPipe({
whitelist: true, // 剔除 DTO 中未声明的属性
forbidNonWhitelisted: true, // 发现未声明属性就报错（可选）
transform: true, // 把请求体转为 DTO 类实例（依赖 class-transformer）
transformOptions: { enableImplicitConversion: true }, // 自动把基本类型转换（string -> number）——可选但常用
})

class-validator 和 class-transformer 参数在官网查阅
class-validator ： https://www.npmjs.com/package/class-validator
class-transformer : https://www.npmjs.com/package/class-transformer

部分常用参数
一、class-transformer 常用参数
主要用于控制属性的暴露/隐藏、类型转换以及重命名。

1. 核心装饰器参数
<!-- 装饰器/参数	                      说明	                                                      代码示例
@Expose()	       暴露属性（通常与 excludeExtraneousValues 配合使用，仅暴露标记过的属性）	  @Expose() id: number;
@Exclude()	        排除属性（转换时忽略该属性）	@Exclude() password: string;
@Type(() => Type)	指定类型：用于处理嵌套对象或数组，告诉程序该属性应转换成哪个类。	      @Type(() => User) users: User[];
@Transform()	   自定义转换：在转换过程中对值进行修改（如将字符串转数字、日期格式化）。	       @Transform(({ value }) => value.name) fullName: string; -->
2. 常用选项参数 (Options)
   在调用 plainToInstance 或 instanceToPlain 时作为第三个参数传入：
   选项 说明 使用场景
   excludeExtraneousValues: true 排除未用 @Expose 装饰的属性。 严格 DTO 定义，防止多余字段入库。
   enableImplicitConversion: true 启用隐式转换（如 "1" -> 1）。 宽松的前端参数处理。
   groups 基于组别控制序列化/反序列化。 不同的 API 接口返回不同的字段集（如管理员与普通用户）。

二、class-validator 常用参数
主要用于校验属性的合法性，如长度、类型、格式等。

1. 常用验证装饰器
<!-- 类别	         装饰器示例	                                               作用
字符串	@Length(min, max), @IsEmail(), @IsUrl(), @Contains()	校验字符串长度、邮箱格式、URL 格式等。
数值	@IsInt(), @Min(n), @Max(n), @IsPositive()	            校验整数、最小值、最大值、正数。
通用	@IsOptional(), @IsEnum(), @IsBoolean(), @IsDate()	    标记字段可选、校验是否为枚举值、布尔值、日期。
数组	@ArrayNotEmpty(), @ArrayUnique()	                    校验数组不为空、数组元素唯一。
嵌套	@ValidateNested()	                                    验证嵌套对象中的规则，通常与 @Type() 配合使用。
条件	@ValidateIf(o => o.other === 'value')	                条件验证：满足条件时才验证该字段。 -->

2. 关键验证选项
   每个装饰器都可接收一个选项对象作为参数，常用参数如下：

<!-- 参数	                       说明	                                                  代码示例
message	自定义错误信息，支持模板（如 $value, $constraint1）。	@Length(5, { message: '长度必须是 $constraint1 到 $constraint2' })
each	设置为 true 时，验证数组/集合中的每一个元素。	         @IsInt({ each: true }) ids: number[];
groups	组别控制：仅在指定的验证组中生效。	                     @Min(1, { groups: ['update'] })
always	设为 false 时，若未指定组则跳过验证。	                 @Min(1, { always: false }) -->

3. 全局验证器选项 (validate 函数参数)
   调用 validate(object, options) 时常用的配置项：

<!-- 选项	                               说明	                                 最佳实践
whitelist: true	            自动剥离 DTO 中未定义装饰器的属性。	           强烈建议：防止注入多余参数。
forbidNonWhitelisted: true	发现未定义属性时直接抛出错误。	               严格模式。
forbidUnknownValues: true	禁止未知对象的验证通过。	                   默认开启，建议保持开启。
stopAtFirstError: true	     任一属性校验失败时立即停止该校验（属性级短路）。减少不必要的校验开销。 -->

连接数据库

使用@nestjs/typeorm 中的 TypeOrmModule/TypeOrmModule 是 Nest.js 提供的一个模块，用于集成 TypeORM，这是一个流行的 ORM（对象关系映射）库，用于与数据库进行交互。通过导入 TypeOrmModule，你可以在 Nest.js 应用中使用 TypeORM 的功能来定义实体、创建数据库连接、执行查询等操作。在 AppModule 中导入 TypeOrmModule 可以让你在整个应用中使用 TypeORM 来管理数据库操作。

使用typeorm 创建表名
//typeorm 是一个流行的 TypeScript ORM（对象关系映射）库，用于与数据库进行交互。它提供了装饰器和实体类来定义数据库表结构和关系。
//Entity 装饰器用于标记一个类为数据库实体，表示该类对应数据库中的一张表。
//Column 装饰器用于定义实体类中的属性对应数据库表中的列。
//PrimaryGeneratedColumn 装饰器用于标记一个属性为主键，并且该主键的值会自动生成。
//CreateDateColumn 装饰器用于标记一个属性为创建时间戳，数据库会自动设置该字段的值为记录创建的时间。
//UpdateDateColumn 装饰器用于标记一个属性为更新时间戳，数据库会自动设置该字段的值为记录最后一次更新的时间。

在module中导入
imports: [TypeOrmModule.forFeature([User])], // 导入 TypeOrmModule，并注册 User 实体，以便在 UsersService 中使用 Repository 来操作 User 实体对应的数据库表。

在srevers中使用
InjectRepository 是 TypeORM 提供的一个装饰器，用于在 Nest.js 中注入一个特定实体的 Repository。 Repository 是 TypeORM 中用于执行数据库操作的类，
使用 InjectRepository(User) 可以将 User 实体的 Repository 注入到 UsersService 中，
定义一个私有属性 userRepository，类型为 Repository<User>，用于在 UsersService 中执行与 User 实体相关的数据库操作。

代码示例

 <!-- async cereateUser(createUserDto: CreateUserDto): Promise<UserResponseDto> {
    const user = this.userRepository.create(createUserDto); // 使用 Repository 的 create 方法创建一个新的 User 实体实例，并将 CreateUserDto 中的数据传递给它。
    const saved = await this.userRepository.save(user); // 保存到数据库并得到完整实体（含敏感或元数据字段）
    // 使用 class-transformer 将实体转换为响应 DTO，并排除未标记的字段
    // plainToInstance 函数接受三个参数：要转换成的类（UserResponseDto）、
    // 要转换的对象（saved），以及一个选项对象。在选项对象中，
    // excludeExtraneousValues: true 表示在转换过程中排除那些没有在 UserResponseDto 中使用 @Expose() 装饰器标记的字段。
    return plainToInstance(UserResponseDto, saved, { excludeExtraneousValues: true });
  } -->
